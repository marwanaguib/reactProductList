import React from "react";

import MainTemplate from "../../templates/main";

const Home = () => {
  return (
    <MainTemplate>
      <div>Home Page</div>
    </MainTemplate>
  );
};

export default Home;
